pages:
# NOTE: `name` field must match the `navbar_title` field in the page's front matter to ensure the correct page is highlighted in the navbar.
#       For example, the `navbar_title` field in `publications.html` is "Publications" and the `name` field here should also be "Publications"
- name: Home
  url: /
- name: Publications
  url: /publications
